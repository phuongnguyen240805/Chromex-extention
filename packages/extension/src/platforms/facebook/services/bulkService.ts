export const bulkService = {
  unfriend: async (userIds: string[]) => {
    console.log('Unfriending:', userIds);
    // Implementation
  },
  deletePosts: async (postIds: string[]) => {
    console.log('Deleting posts:', postIds);
    // Implementation
  }
};
